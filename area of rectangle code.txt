1st
//to find the area of rectangle 
#include <stdio.h>
void main()
{
	float l,w,area;
	printf("enter the value of l and w:");
	scanf("%f%f",&l,&w);
	area=l*w;
	printf("area: %f",area);

}



2nd 
//to find the parimeter of rectangle 
#include <stdio.h>
    void main()
{
float l,w,parimeter;
	printf("enter the value of l and w:");
	scanf("%f%f",&l,&w);
	parimeter=2*(l+w);
	printf("parimeter: %f",parimeter);
}