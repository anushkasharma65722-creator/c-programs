#include <stdio.h>
void main() {
   int n,i=1,count=0;
  
    printf(" Enter the no:");
   scanf ("%d",&n);
   while (i<=n)
   { 
       if(n%i==0)
       {
           count++;
       }
   i++;
  }
   if(count==2)
       printf("no is prime"); 
   else 
   printf("no is not prime");
   
}